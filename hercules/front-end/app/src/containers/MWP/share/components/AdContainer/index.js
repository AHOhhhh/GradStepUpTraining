import React from 'react'
import { ReferenceOrderAd, ReferenceOrderBanner } from '../../../../shared/ReferenceOrderContainer'

export const renderReferenceAdBanner = (order) => <ReferenceOrderAd order={order} type="mwp" />

export const renderReferenceOrderBanner = (order) => <ReferenceOrderBanner order={order} type="mwp" />
